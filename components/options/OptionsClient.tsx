"use client";

import { OptionsBuilder } from "./OptionsBuilder";

/** Options tab — the analysis-only strategy builder (no order placement). */
export function OptionsClient() {
  return <OptionsBuilder />;
}
